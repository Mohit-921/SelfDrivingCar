import sys

#it does something which is related to the cmd environ, ignore!

# Print command line arguments
print("Number of command line arguments:", len(sys.argv))
print("Command line arguments are:", sys.argv)

# Exit the program with a custom exit code
sys.exit(0)
